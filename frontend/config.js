// Points the Vercel-hosted UI at the current backend tunnel.
// EPHEMERAL: the cloudflared quick-tunnel URL changes every time you re-run
// scripts/run-demo.sh. When it changes, update this line and redeploy
// (`vercel deploy --prod --yes`), OR just use the ?api= link the script prints.
window.SCAMSHIELD_API = "https://otherwise-statutory-history-objects.trycloudflare.com";
